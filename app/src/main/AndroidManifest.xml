<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.github.ihsg.appfoundation">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".sign.LoginActivity"
            android:configChanges="orientation|keyboardHidden|screenSize" />
        <activity
            android:name=".paging.page.LoanListActivity"
            android:theme="@style/TranslateStatusTheme" />
        <activity android:name=".paging.db.StudentListActivity" />
        <activity android:name=".onboarding.OnBoardingActivity" />
        <activity android:name=".splash.SplashActivity" />
        <activity
            android:name=".main.MainActivity"
            android:launchMode="singleTask" />

        <activity-alias
            android:name=".launcher"
            android:targetActivity=".paging.page.LoanListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>
    </application>
</manifest>